# Copy to .env.local and fill in. Only these two values may reach the browser.
# Never put the secret / service-role key here — it bypasses RLS.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
